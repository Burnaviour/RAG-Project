[project]
name = "langrag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph",
    "langchain",
    "langchain_openai",
    "dotenv",
    "streamlit>=1.50.0",
    "streamlit-chat>=0.1.1",
]
